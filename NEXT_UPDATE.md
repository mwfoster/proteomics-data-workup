# Next update

- Move the CV plot to the upper-right area of the CV tab.
- Add a statistics control for choosing the metadata field used to match paired samples. Keep `Replicate` as the default; suitable alternatives include `Subject` or another metadata column whose values are nonblank, unique within each condition, and shared by both comparison groups.
